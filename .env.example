# Optional but recommended: persists Discord alerts across requests.
# Create a free KV store in your Vercel dashboard (Storage tab -> Create -> KV),
# then copy these two values in. Without them, the site still works — Discord
# alerts just reset when the serverless function cold-starts.
KV_REST_API_URL=
KV_REST_API_TOKEN=
